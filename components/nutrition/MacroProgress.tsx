import type { MacroTotals } from "@/modules/nutrition/nutrition.types";
function Row({ label, value, goal, unit }: { label: string; value: number; goal: number; unit: string }) { const pct = Math.min(100, Math.round((value / goal) * 100)); return <div><div className="macro-progress__heading"><strong>{label}</strong><span className="muted">{value}/{goal}{unit}</span></div><progress className="progress macro-progress__bar" value={pct} max={100}>{pct}%</progress></div>; }
export function MacroProgress({ consumed, goal }: { consumed: MacroTotals; goal: MacroTotals }) { return <div className="card"><h2>Macros de hoy</h2><div className="grid"><Row label="Calorías" value={consumed.calories} goal={goal.calories} unit=" kcal"/><Row label="Proteína" value={consumed.proteinG} goal={goal.proteinG} unit=" g"/><Row label="Carbohidratos" value={consumed.carbsG} goal={goal.carbsG} unit=" g"/><Row label="Grasas" value={consumed.fatG} goal={goal.fatG} unit=" g"/></div></div>; }
