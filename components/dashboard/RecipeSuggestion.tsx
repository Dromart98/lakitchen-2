import type { GeneratedRecipe } from "@/modules/recipes/recipes.types";
export function RecipeSuggestion({ recipe }: { recipe: GeneratedRecipe }) { return <div className="card"><h2>Receta sugerida</h2><p className="pill">score {recipe.score}</p><h3>{recipe.name}</h3><p className="muted">{recipe.calories} kcal · P {recipe.proteinG}g · C {recipe.carbsG}g · G {recipe.fatG}g</p><ul>{recipe.ingredients.map((ingredient) => <li key={ingredient.name}>{ingredient.quantity} {ingredient.unit} de {ingredient.name}</li>)}</ul><button className="button">Marcar como preparada</button></div>; }
