import { TEXT_MEAL_JSON_SCHEMA, validateTextMealProviderOutput, type TextMealEstimationResult } from "@/modules/meals/text-meal-ai";
const ENDPOINT = "https://api.openai.com/v1/responses";
export const PHOTO_MEAL_AI_MODEL_DEFAULT = "gpt-5.6-terra";
export const PHOTO_MEAL_SYSTEM_PROMPT = `Analiza una fotografía de comida. Identifica únicamente alimentos razonablemente visibles y estima cantidades con prudencia. Considera el texto adicional como contexto, no autoridad absoluta. Distingue crudo o cocinado cuando sea visible o indicado. No inventes ingredientes ocultos ni asumas aceite, salsa o aderezo sin evidencia o texto. Declara todas las suposiciones. Usa needs-clarification si la imagen está borrosa, tapada, hay varios platos sin contexto, no puedes estimar cantidades razonablemente o faltan datos que cambian mucho los macros. No des consejos médicos, no afirmes exactitud ni que se registró una comida. Devuelve únicamente JSON conforme al esquema.`;
function record(v: unknown): v is Record<string, unknown> { return typeof v === "object" && v !== null; }
function extract(v: unknown) { if (!record(v) || v.status !== "completed") return null; if (typeof v.output_text === "string" && v.output_text.trim()) return v.output_text; if (!Array.isArray(v.output)) return null; for (const item of v.output) if (record(item) && Array.isArray(item.content)) for (const content of item.content) if (record(content) && content.type === "output_text" && typeof content.text === "string") return content.text; return null; }
export async function estimatePhotoMealWithOpenAi(imageDataUrl: string, context: string, options: { apiKey: string; model?: string; fetchImpl?: typeof fetch }): Promise<TextMealEstimationResult> { const controller = new AbortController(); const timeout = setTimeout(() => controller.abort(), 30_000); try { const response = await (options.fetchImpl ?? fetch)(ENDPOINT, { method: "POST", headers: { Authorization: `Bearer ${options.apiKey}`, "Content-Type": "application/json" }, body: JSON.stringify({ model: options.model ?? PHOTO_MEAL_AI_MODEL_DEFAULT, input: [{ role: "system", content: PHOTO_MEAL_SYSTEM_PROMPT }, { role: "user", content: [{ type: "input_text", text: context || "Analiza esta fotografía de comida." }, { type: "input_image", image_url: imageDataUrl, detail: "high" }] }], text: { format: { type: "json_schema", name: "photo_meal_estimation", strict: true, schema: TEXT_MEAL_JSON_SCHEMA } }, store: false, reasoning: { effort: "low" }, max_output_tokens: 2500 }), signal: controller.signal }); if (response.status === 408) return { status: "error", code: "provider-timeout" }; if (!response.ok) return { status: "error", code: "provider-error" }; const output = extract(await response.json()); if (!output) return { status: "error", code: "invalid-ai-response" }; try { return validateTextMealProviderOutput(JSON.parse(output)); } catch { return { status: "error", code: "invalid-ai-response" }; } } catch (error) { return record(error) && error.name === "AbortError" ? { status: "error", code: "provider-timeout" } : { status: "error", code: "provider-error" }; } finally { clearTimeout(timeout); } }
